# OpenCode插件系统技术设计文档

**日期**: 2026-01-26 17:00  
**责任人**: IcySaint  
**版本**: v1.0

---

## 一、背景与目标

### 1.1 背景
OpenCode是一个开源的AI编码助手，支持通过插件系统扩展功能。当前OpenCode Config Manager已支持Provider、Model、MCP、Agent、Skill等配置管理，但缺少对OpenCode官方插件系统的可视化管理功能。

### 1.2 目标
- 实现OpenCode插件的可视化管理界面
- 支持从npm安装插件（自动安装）
- 支持从本地目录加载插件
- 支持插件的启用/禁用/卸载
- 支持插件更新检测和更新
- 完全符合OpenCode官方插件规范

---

## 二、OpenCode插件系统规范

### 2.1 插件加载方式

OpenCode支持两种插件加载方式：

#### **方式1：npm包插件**
- 在`opencode.json`的`plugin`字段中指定npm包名
- 支持普通包和scoped包（如`@my-org/custom-plugin`）
- 支持指定版本（如`opencode-skills@0.1.0`）
- 启动时自动使用Bun安装
- 缓存目录：`~/.cache/opencode/node_modules/`

**配置示例**：
```json
{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    "opencode-helicone-session",
    "opencode-wakatime",
    "@my-org/custom-plugin",
    "opencode-skills@0.1.0"
  ]
}
```

#### **方式2：本地文件插件**
- 放置在插件目录中的`.js`或`.ts`文件
- 项目级插件：`.opencode/plugins/`
- 全局插件：`~/.config/opencode/plugins/`
- 启动时自动加载

### 2.2 插件加载顺序

插件按以下顺序加载，所有hooks按顺序执行：

1. 全局配置（`~/.config/opencode/opencode.json`）
2. 项目配置（`opencode.json`）
3. 全局插件目录（`~/.config/opencode/plugins/`）
4. 项目插件目录（`.opencode/plugins/`）

**注意**：
- 相同名称和版本的npm包只加载一次
- 本地插件和npm插件即使名称相似也会分别加载

### 2.3 插件依赖管理

- **npm插件**：依赖自动安装到`~/.cache/opencode/node_modules/`
- **本地插件**：需要在配置目录创建`package.json`管理依赖，或发布到npm

### 2.4 插件Hook系统

OpenCode插件可以hook到各种事件：

- **生命周期hooks**：`onLoad`、`onUnload`
- **会话hooks**：`onSessionStart`、`onSessionEnd`
- **消息hooks**：`onMessageSent`、`onMessageReceived`
- **工具hooks**：`onToolCall`、`onToolResult`
- **文件hooks**：`onFileRead`、`onFileWrite`
- **自定义工具注册**：`registerTool()`

---

## 三、功能设计

### 3.1 核心功能模块

#### **模块1：插件列表管理**
- 显示已安装的npm插件和本地插件
- 显示插件状态（已启用/已禁用）
- 显示插件版本、描述、来源
- 支持搜索和筛选

#### **模块2：插件安装**
- **npm安装**：输入npm包名，添加到`opencode.json`的`plugin`数组
- **本地安装**：选择本地`.js`/`.ts`文件，复制到插件目录
- 支持指定版本（如`package@version`）
- 安装后自动刷新列表

#### **模块3：插件配置管理**
- 读取`opencode.json`的`plugin`字段
- 支持启用/禁用插件（添加/移除数组项）
- 支持卸载插件（从配置中移除）
- 保存配置到`opencode.json`

#### **模块4：插件更新检测**
- 检测npm插件的最新版本（通过npm registry API）
- 对比当前版本和最新版本
- 支持一键更新（修改版本号或移除版本号使用latest）

#### **模块5：插件市场（可选）**
- 预设常用插件列表（opencode-helicone-session、opencode-wakatime、opencode-skills等）
- 一键安装预设插件

### 3.2 数据结构设计

#### **插件配置数据结构**
```python
@dataclass
class PluginConfig:
    """插件配置数据类"""
    name: str              # 插件名称（npm包名或文件名）
    version: str           # 版本号（npm插件）
    type: str              # 类型：npm / local
    source: str            # 来源：npm包名 / 本地文件路径
    enabled: bool          # 是否启用
    description: str       # 描述
    homepage: str          # 主页链接
    installed_at: str      # 安装时间
```

#### **插件元数据文件**
为本地插件创建`.plugin-meta.json`，记录安装信息：
```json
{
  "name": "my-custom-plugin",
  "version": "1.0.0",
  "type": "local",
  "source": "/path/to/plugin.js",
  "installed_at": "2026-01-26T17:00:00",
  "description": "My custom OpenCode plugin"
}
```

### 3.3 UI设计

#### **页面布局**
```
┌─────────────────────────────────────────────────────────┐
│  Plugin 插件管理                                         │
├─────────────────────────────────────────────────────────┤
│  [🔍 搜索]  [➕ 安装插件]  [🔄 检查更新]  [🛒 插件市场]  │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │ 插件名称          │ 版本  │ 类型 │ 状态 │ 操作 │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ opencode-skills   │ 0.1.0 │ npm  │ ✅   │ [⚙️][🗑️]│   │
│  │ opencode-wakatime │ 1.2.3 │ npm  │ ✅   │ [⚙️][🗑️]│   │
│  │ my-plugin.js      │ -     │ local│ ✅   │ [⚙️][🗑️]│   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### **安装对话框**
```
┌─────────────────────────────────────┐
│  安装插件                            │
├─────────────────────────────────────┤
│  安装方式：                          │
│  ○ 从npm安装                         │
│  ○ 从本地文件安装                    │
│                                      │
│  npm包名：                           │
│  [opencode-skills@0.1.0        ]    │
│                                      │
│  或                                  │
│                                      │
│  本地文件：                          │
│  [选择文件...]                       │
│                                      │
│  [取消]  [安装]                      │
└─────────────────────────────────────┘
```

---

## 四、技术实现方案

### 4.1 核心类设计

#### **PluginManager类**
```python
class PluginManager:
    """插件管理器"""
    
    @staticmethod
    def get_installed_plugins() -> List[PluginConfig]:
        """获取已安装的插件列表"""
        # 1. 读取opencode.json的plugin字段（npm插件）
        # 2. 扫描~/.config/opencode/plugins/目录（本地插件）
        # 3. 扫描.opencode/plugins/目录（项目插件）
        # 4. 合并并返回
        pass
    
    @staticmethod
    def install_npm_plugin(package_name: str) -> bool:
        """安装npm插件"""
        # 1. 解析包名和版本（如opencode-skills@0.1.0）
        # 2. 读取opencode.json
        # 3. 添加到plugin数组
        # 4. 保存配置
        pass
    
    @staticmethod
    def install_local_plugin(file_path: str, target_dir: str) -> bool:
        """安装本地插件"""
        # 1. 复制文件到目标目录
        # 2. 创建.plugin-meta.json
        pass
    
    @staticmethod
    def uninstall_plugin(plugin: PluginConfig) -> bool:
        """卸载插件"""
        # npm插件：从opencode.json的plugin数组移除
        # 本地插件：删除文件和元数据
        pass
    
    @staticmethod
    def check_updates() -> Dict[str, str]:
        """检查插件更新"""
        # 通过npm registry API检查最新版本
        pass
```

#### **PluginPage类**
```python
class PluginPage(BasePage):
    """插件管理页面"""
    
    def __init__(self, main_window):
        super().__init__(main_window)
        self._setup_ui()
        self._load_plugins()
    
    def _setup_ui(self):
        """初始化UI"""
        # 搜索框、按钮、表格
        pass
    
    def _load_plugins(self):
        """加载插件列表"""
        plugins = PluginManager.get_installed_plugins()
        self._update_table(plugins)
    
    def _on_install(self):
        """安装插件"""
        dialog = PluginInstallDialog(self)
        if dialog.exec():
            self._load_plugins()
    
    def _on_uninstall(self):
        """卸载插件"""
        # 确认对话框 → 卸载 → 刷新列表
        pass
```

### 4.2 配置读写逻辑

#### **读取插件配置**
```python
def get_plugins_from_config() -> List[str]:
    """从opencode.json读取plugin字段"""
    config = load_opencode_config()
    return config.get("plugin", [])
```

#### **写入插件配置**
```python
def save_plugins_to_config(plugins: List[str]):
    """保存plugin字段到opencode.json"""
    config = load_opencode_config()
    config["plugin"] = plugins
    save_opencode_config(config)
```

### 4.3 npm插件版本检测

```python
import requests

def get_latest_version(package_name: str) -> str:
    """获取npm包的最新版本"""
    url = f"https://registry.npmjs.org/{package_name}/latest"
    response = requests.get(url)
    if response.status_code == 200:
        data = response.json()
        return data.get("version", "")
    return ""
```

---

## 五、预设插件市场

### 5.1 常用插件列表

| 插件名称 | 描述 | npm包名 | 官方链接 |
|---------|------|---------|---------|
| OpenCode Skills | 自动发现和注册Skills为动态工具 | `opencode-skills` | [GitHub](https://github.com/malhashemi/opencode-skills) |
| OpenCode Sessions | 多Agent协作和工作流编排 | `opencode-sessions` | [GitHub](https://github.com/malhashemi/opencode-sessions) |
| Helicone Session | 注入Helicone会话ID和名称 | `opencode-helicone-session` | - |
| WakaTime | 代码时间追踪 | `opencode-wakatime` | - |

### 5.2 插件市场数据结构

```python
PRESET_PLUGINS = [
    {
        "name": "opencode-skills",
        "display_name": "OpenCode Skills",
        "description": "自动发现和注册Skills为动态工具，支持Anthropic Agent Skills规范",
        "npm_package": "opencode-skills",
        "homepage": "https://github.com/malhashemi/opencode-skills",
        "category": "工具增强",
    },
    {
        "name": "opencode-sessions",
        "display_name": "OpenCode Sessions",
        "description": "多Agent协作和工作流编排，支持回合制讨论和并行探索",
        "npm_package": "opencode-sessions",
        "homepage": "https://github.com/malhashemi/opencode-sessions",
        "category": "协作增强",
    },
    # ... 更多预设插件
]
```

---

## 六、开发计划

### 6.1 开发阶段

#### **阶段1：核心功能实现**（优先级：高）
- [ ] 实现PluginManager类
- [ ] 实现插件配置读写逻辑
- [ ] 实现PluginPage UI
- [ ] 实现npm插件安装/卸载
- [ ] 实现本地插件安装/卸载

#### **阶段2：增强功能**（优先级：中）
- [ ] 实现插件更新检测
- [ ] 实现插件市场
- [ ] 实现插件搜索和筛选
- [ ] 实现插件详情对话框

#### **阶段3：优化和文档**（优先级：低）
- [ ] 性能优化
- [ ] 错误处理优化
- [ ] 编写用户文档
- [ ] 编写开发文档

### 6.2 测试计划

- **单元测试**：PluginManager的各个方法
- **集成测试**：安装/卸载/更新流程
- **UI测试**：页面交互和数据显示
- **兼容性测试**：Windows/macOS/Linux

---

## 七、风险与注意事项

### 7.1 技术风险

1. **npm registry访问限制**：可能遇到API rate limit
   - **解决方案**：添加缓存机制，限制请求频率

2. **本地插件依赖管理**：本地插件可能依赖外部包
   - **解决方案**：提示用户创建package.json或发布到npm

3. **插件冲突**：多个插件可能hook同一事件
   - **解决方案**：按加载顺序执行，文档中说明

### 7.2 用户体验风险

1. **npm安装失败**：网络问题或包不存在
   - **解决方案**：详细的错误提示和重试机制

2. **配置文件损坏**：手动编辑导致格式错误
   - **解决方案**：配置验证和自动修复

### 7.3 安全注意事项

1. **插件代码安全**：插件可以执行任意代码
   - **提示**：在UI中明确警告用户只安装可信插件

2. **npm包劫持**：恶意npm包
   - **提示**：显示插件来源和下载量，提醒用户验证

---

## 八、相关文件

### 8.1 核心代码文件
- `opencode_config_manager_fluent.py`：主程序文件
- 新增：`PluginManager`类（约200行）
- 新增：`PluginPage`类（约300行）
- 新增：`PluginInstallDialog`类（约150行）

### 8.2 配置文件
- `~/.config/opencode/opencode.json`：OpenCode配置文件
- `~/.config/opencode/plugins/`：全局插件目录
- `.opencode/plugins/`：项目插件目录

### 8.3 文档文件
- `docs/technical/OpenCode插件系统技术设计文档.md`：本文档
- `docs/feature/插件管理功能完成报告.md`：功能完成报告（待创建）
- `docs/guide/插件管理使用指南.md`：用户使用指南（待创建）

---

## 九、参考资料

### 9.1 官方文档
- [OpenCode Plugins Documentation](https://opencode.ai/docs/plugins/)
- [OpenCode Configuration Schema](https://opencode.ai/config.json)
- [OpenCode Skills Plugin](https://github.com/malhashemi/opencode-skills)
- [OpenCode Sessions Plugin](https://github.com/malhashemi/opencode-sessions)

### 9.2 社区资源
- [OpenCode Plugin Ecosystem](https://opencode.ai/docs/plugins/#ecosystem)
- [DEV Community: OpenCode Hooks Guide](https://dev.to/einarcesar/does-opencode-support-hooks-a-complete-guide-to-extensibility-k3p)

---

## 十、版本历史

| 版本 | 日期 | 修改内容 | 责任人 |
|------|------|---------|--------|
| v1.0 | 2026-01-26 | 初始版本，完成技术设计 | IcySaint |

---

**文档状态**：✅ 已完成  
**下一步**：开始实现PluginManager类和PluginPage UI
