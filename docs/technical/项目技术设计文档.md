# OpenCode 配置管理器 - 技术设计文档

## 日期
2026-01-13 15:30

## 责任人
AI 辅助开发

---

## 一、项目概述

### 1.1 项目名称
**OpenCode Config Manager** (opcdcfg)

### 1.2 项目定位
一个单文件、带 GUI 的 Python 脚本工具，用于可视化管理 OpenCode 和 Oh My OpenCode 的配置文件，面向小白用户，降低配置门槛。

### 1.3 核心目标
- 零门槛配置：用户无需了解 JSON 格式，点击即可完成配置
- 一键导入：支持从 Claude Code、Codex 等工具导入已有配置
- 可视化管理：所有配置项以图形界面展示，支持增删改查
- 安全可靠：配置修改前自动备份，支持一键回滚

---

## 二、功能需求分析

### 2.1 核心功能模块

| 模块编号 | 模块名称 | 功能描述 | 优先级 |
|---------|---------|---------|--------|
| M1 | 配置读取 | 读取本地 opencode.json 和 oh-my-opencode.json | P0 |
| M2 | 配置写入 | 保存修改后的配置到文件 | P0 |
| M3 | 外部导入 | 读取 Claude Code、Codex 等配置并导入 | P1 |
| M4 | Provider 管理 | 添加/编辑/删除自定义 API 站点 | P0 |
| M5 | Model 管理 | 添加/编辑/删除自定义模型 | P0 |
| M6 | Agent 管理 | 配置 oh-my-opencode 的 agents | P1 |
| M7 | Category 管理 | 配置 oh-my-opencode 的 categories | P1 |
| M8 | 权限管理 | 配置 opencode 的 permission 项 | P1 |
| M9 | MCP 管理 | 配置 MCP 服务器 | P2 |
| M10 | 主题设置 | 配置 opencode 主题 | P2 |
| M11 | 备份恢复 | 配置文件备份与恢复 | P1 |

### 2.2 配置文件结构

#### 2.2.1 opencode.json 核心配置项

```json
{
  "$schema": "https://opencode.ai/config.json",
  "theme": "string",           // 主题名称
  "permission": {              // 权限配置
    "工具名": "allow|ask|deny"
  },
  "compaction": {              // 压缩配置
    "auto": true,
    "prune": true
  },
  "mcp": {                     // MCP 服务器配置
    "服务名": {
      "type": "local|remote",
      "command": ["命令", "参数"],
      "enabled": true
    }
  },
  "plugin": ["插件名"],        // 插件列表
  "provider": {                // API 提供商配置
    "提供商名": {
      "npm": "@ai-sdk/xxx",
      "name": "显示名称",
      "options": {
        "baseURL": "API地址",
        "apiKey": "密钥"
      },
      "models": {
        "模型ID": {
          "name": "模型名称",
          "attachment": true,
          "limit": { "context": 1048576, "output": 65535 },
          "modalities": { "input": ["text", "image"], "output": ["text"] },
          "options": {}
        }
      }
    }
  }
}
```

#### 2.2.2 oh-my-opencode.json 核心配置项

```json
{
  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-opencode/master/assets/oh-my-opencode.schema.json",
  "google_auth": false,
  "agents": {
    "agent名称": {
      "model": "provider/model-id",
      "description": "描述"
    }
  },
  "sisyphus_agent": {
    "disabled": false,
    "default_builder_enabled": false,
    "planner_enabled": true,
    "replace_plan": true
  },
  "background_task": {
    "defaultConcurrency": 5
  },
  "categories": {
    "分类名": {
      "model": "provider/model-id",
      "temperature": 0.7,
      "description": "描述"
    }
  },
  "disabled_hooks": [],
  "disabled_skills": [],
  "disabled_agents": []
}
```

---

## 三、技术架构设计

### 3.1 技术选型

| 技术项 | 选型 | 理由 |
|-------|------|------|
| 编程语言 | Python 3.8+ | 跨平台、生态丰富、用户基础广 |
| GUI 框架 | tkinter + ttk | Python 内置、无需额外安装、单文件友好 |
| 配置解析 | json (内置) | 原生支持、无依赖 |
| TOML 解析 | 自实现简易解析器 | 避免外部依赖 |

### 3.2 模块架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        主窗口 (MainWindow)                        │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  菜单栏     │  │  工具栏     │  │  状态栏     │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
├─────────────────────────────────────────────────────────────────┤
│                      选项卡容器 (Notebook)                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Tab1: Provider管理 │ Tab2: Model管理 │ Tab3: Agent管理    │  │
│  │ Tab4: Category管理 │ Tab5: 权限管理  │ Tab6: MCP管理      │  │
│  │ Tab7: 外部导入     │ Tab8: 高级设置  │                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        核心服务层                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ ConfigPaths │  │ConfigManager│  │ DataValidator│              │
│  │ 路径管理    │  │ 读写管理    │  │ 数据校验    │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │BackupManager│  │ImportService│  │ ModelRegistry│              │
│  │ 备份管理    │  │ 导入服务    │  │ 模型注册表  │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 类设计

#### 3.3.1 核心类列表

| 类名 | 职责 | 依赖 |
|-----|------|------|
| `ConfigPaths` | 管理所有配置文件路径 | 无 |
| `ConfigManager` | 配置文件读写操作 | ConfigPaths |
| `BackupManager` | 配置备份与恢复 | ConfigManager |
| `DataValidator` | 配置数据校验 | 无 |
| `ImportService` | 外部配置导入 | ConfigManager |
| `ModelRegistry` | 可用模型注册表 | ConfigManager |
| `MainWindow` | 主窗口 | 所有服务类 |
| `ProviderTab` | Provider 管理界面 | MainWindow |
| `ModelTab` | Model 管理界面 | MainWindow |
| `AgentTab` | Agent 管理界面 | MainWindow |
| `CategoryTab` | Category 管理界面 | MainWindow |
| `PermissionTab` | 权限管理界面 | MainWindow |
| `McpTab` | MCP 管理界面 | MainWindow |
| `ImportTab` | 外部导入界面 | MainWindow |
| `SettingsTab` | 高级设置界面 | MainWindow |

---

## 四、GUI 界面设计

### 4.1 主窗口布局

```
┌──────────────────────────────────────────────────────────────────────┐
│ 文件(F)  编辑(E)  工具(T)  帮助(H)                                    │
├──────────────────────────────────────────────────────────────────────┤
│ [💾保存] [↩️撤销] [🔄刷新] [📦备份] [📥导入]                          │
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────┬────────┬────────┬────────┬────────┬────────┬────────┐    │
│ │Provider│ Model  │ Agent  │Category│ 权限   │  MCP   │ 导入   │    │
│ └────────┴────────┴────────┴────────┴────────┴────────┴────────┘    │
│ ┌──────────────────────────────────────────────────────────────────┐│
│ │                                                                  ││
│ │                      选项卡内容区域                               ││
│ │                                                                  ││
│ │                                                                  ││
│ └──────────────────────────────────────────────────────────────────┘│
├──────────────────────────────────────────────────────────────────────┤
│ 状态: 已加载配置 | 上次保存: 2026-01-13 15:30 | Provider: 5 | Model: 8│
└──────────────────────────────────────────────────────────────────────┘
```

### 4.2 Provider 管理界面

```
┌──────────────────────────────────────────────────────────────────────┐
│ Provider 列表                                    [➕添加] [✏️编辑] [🗑️删除] │
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ ☑ mikey        │ T中转站           │ 1 个模型  │ ✅ 已启用    │  │
│ │ ☑ backup1      │ T2中转站          │ 2 个模型  │ ✅ 已启用    │  │
│ │ ☑ backup2      │ anyrouter         │ 2 个模型  │ ✅ 已启用    │  │
│ │ ☐ backup3      │ cpass             │ 1 个模型  │ ⚠️ 已禁用    │  │
│ └────────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│ Provider 详情                                                        │
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ 名称:     [mikey                    ]                          │  │
│ │ 显示名:   [T中转站                  ]                          │  │
│ │ SDK:      [@ai-sdk/anthropic     ▼]                           │  │
│ │ API地址:  [https://www.tribiosapi.top/v1                    ]  │  │
│ │ API密钥:  [sk-**********************] [👁️显示]                │  │
│ └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

### 4.3 Model 管理界面

```
┌──────────────────────────────────────────────────────────────────────┐
│ 选择 Provider: [mikey ▼]                        [➕添加模型] [🗑️删除] │
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ 模型ID                    │ 显示名称              │ 附件支持   │  │
│ ├────────────────────────────────────────────────────────────────┤  │
│ │ claude-opus-4-5-20251101  │ claude-opus-4-5       │ ✅ 是     │  │
│ │ claude-sonnet-4-5-20250929│ claude-sonnet-4-5     │ ✅ 是     │  │
│ └────────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│ 模型详情                                                             │
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ 模型ID:      [claude-opus-4-5-20251101        ]                │  │
│ │ 显示名称:    [claude-opus-4-5-20251101        ]                │  │
│ │ 附件支持:    [✅] 启用                                          │  │
│ │ 上下文限制:  [1048576    ] tokens                               │  │
│ │ 输出限制:    [65535      ] tokens                               │  │
│ │ 输入模态:    [☑ text] [☑ image] [☐ audio]                      │  │
│ │ 输出模态:    [☑ text] [☐ image] [☐ audio]                      │  │
│ └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

### 4.4 Agent 管理界面 (Oh My OpenCode)

```
┌──────────────────────────────────────────────────────────────────────┐
│ Agent 列表                                       [➕添加] [✏️编辑] [🗑️删除] │
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ Agent 名称        │ 绑定模型                    │ 描述         │  │
│ ├────────────────────────────────────────────────────────────────┤  │
│ │ oracle            │ mikey/claude-opus-4-5       │ 架构专家     │  │
│ │ librarian         │ backup1/claude-sonnet-4-5   │ 文档专家     │  │
│ │ explore           │ backup1/claude-sonnet-4-5   │ 探索专家     │  │
│ │ code-reviewer     │ backup2/claude-opus-4-5     │ 审查专家     │  │
│ └────────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│ Agent 详情                                                           │
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ Agent 名称:  [oracle                          ]                │  │
│ │ 绑定模型:    [mikey/claude-opus-4-5-20251101 ▼]                │  │
│ │              (仅显示 opencode.json 中已配置的模型)              │  │
│ │ 描述:        [架构设计、代码审查、策略规划专家                 ]│  │
│ └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

### 4.5 外部导入界面

```
┌──────────────────────────────────────────────────────────────────────┐
│ 检测到的外部配置                                          [🔄刷新检测] │
├──────────────────────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ 来源              │ 配置路径                    │ 状态         │  │
│ ├────────────────────────────────────────────────────────────────┤  │
│ │ Claude Code       │ ~/.claude/settings.json     │ ✅ 已检测    │  │
│ │ Claude Providers  │ ~/.claude/providers.json    │ ✅ 已检测    │  │
│ │ Codex             │ ~/.codex/config.toml        │ ❌ 未找到    │  │
│ └────────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│ Claude Code 配置预览                                                 │
│ ┌────────────────────────────────────────────────────────────────┐  │
│ │ 发现 3 个 Provider 配置:                                        │  │
│ │   ☑ anthropic (官方)     - API Key: sk-ant-***                 │  │
│ │   ☑ openrouter           - API Key: sk-or-***                  │  │
│ │   ☐ custom-provider      - API Key: sk-***                     │  │
│ │                                                                │  │
│ │ [📥 导入选中项到 OpenCode]                                      │  │
│ └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 五、数据流设计

### 5.1 配置加载流程

```
启动程序
    │
    ▼
ConfigPaths.get_opencode_config()
    │
    ▼
ConfigManager.load_json()
    │
    ├─── 成功 ──→ 解析配置 → 填充 GUI
    │
    └─── 失败 ──→ 创建默认配置 → 填充 GUI
```

### 5.2 配置保存流程

```
用户点击保存
    │
    ▼
DataValidator.validate()
    │
    ├─── 校验失败 ──→ 显示错误提示 → 返回
    │
    └─── 校验成功 ──→ BackupManager.backup()
                          │
                          ▼
                    ConfigManager.save_json()
                          │
                          ├─── 成功 ──→ 更新状态栏
                          │
                          └─── 失败 ──→ 显示错误 → 提示恢复备份
```

### 5.3 外部导入流程

```
用户点击刷新检测
    │
    ▼
ImportService.scan_external_configs()
    │
    ▼
显示检测结果列表
    │
    ▼
用户选择要导入的项
    │
    ▼
ImportService.import_selected()
    │
    ▼
DataValidator.validate_import()
    │
    ├─── 冲突 ──→ 显示冲突对话框 → 用户选择覆盖/跳过/合并
    │
    └─── 无冲突 ──→ 合并到当前配置 → 刷新 GUI
```

---

## 六、相关文件

| 文件路径 | 用途 |
|---------|------|
| `D:\opcdcfg\opencode_config_manager.py` | 主程序文件（单文件） |
| `D:\opcdcfg\docs\technical\项目技术设计文档.md` | 本文档 |
| `D:\opcdcfg\docs\feature\任务分块表.md` | 开发任务分解 |
| `C:\Users\Administrator\.config\opencode\opencode.json` | OpenCode 配置文件 |
| `C:\Users\Administrator\.config\opencode\oh-my-opencode.json` | Oh My OpenCode 配置文件 |

---

## 七、版本信息

- 文档版本: v1.0.0
- 项目版本: v0.1.0 (开发中)
- 发布状态: 设计阶段
